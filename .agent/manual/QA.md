# QA

이 문서는 작업 중 필요한 질의응답을 append 형식으로 누적 기록하는 곳이다.

## 사용 규칙

- 에이전트는 필요한 도메인 정보가 `reference/`에 없을 때 질문을 남긴다.
- 사용자는 같은 문서 마지막 줄 또는 마지막 항목 기준으로 답변을 추가한다.
- 후속 작업 시 에이전트는 가장 마지막 사용자 답변을 우선 참조한다.
- 경로, 파일명, 규칙 언급은 저장소 루트 기준 상대경로를 사용한다.

## 기록 템플릿

```text
Q: {에이전트 질문}
A: {사용자 답변}
```
